import 'package:flutter/material.dart';

class WebPage extends StatelessWidget {
  const WebPage({super.key});

  static const String route = '/web';

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('web Page'));
  }
}
